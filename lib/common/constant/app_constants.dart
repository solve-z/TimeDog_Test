import 'package:flutter/material.dart';

class AppConstants {
  static const double appBarIconSize = 30.0;
  static const double bottomNavIconSize = 28.0;
  static const double actionButtonIconSize = 32.0;
}

class AssetPaths {
  static const String iconPath = 'assets/images/icons/';

  static const String crownIcon = '${iconPath}crown.svg';
  static const String listIcon = '${iconPath}list.svg';
  static const String plusIcon = '${iconPath}plus.svg';
  static const String settingIcon = '${iconPath}setting.svg';

  static const String timerIcon = '${iconPath}timer.svg';
  static const String diaryIcon = '${iconPath}diary.svg';
  static const String chartIcon = '${iconPath}chart.svg';
  static const String peopleIcon = '${iconPath}people.svg';
}

class AppColors {
  static const Color selectedBottomNav = Color(0xFFF67575);
  static const Color scaffoldBackground = Color(0xFFFFFFFF);
  static const Color bottomNavBackground = Color(0xFFFFFFFF);
  static const Color appBarBackground = Color(0xFFFFFFFF);
}
